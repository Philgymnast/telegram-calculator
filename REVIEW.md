# Отзыв по проекту «telegram-calculator»

## Phil (Philgymnast)

**Что сделал:** инициализация репозитория, `README.md` с распределением обязанностей, основа бота в `file.py` (команды `/start`, `/help`, polling).

**Сильные стороны:**
- Выбрал подходящую библиотеку (`python-telegram-bot`).
- Корректная структура основного цикла (Updater, dispatcher, handlers).
- Есть документация с ролями в команде.

**Что можно улучшить:**
- Токен стоит переместить в `.env` — в публичном репозитории опасно держать его в коде.
- Имя файла `file.py` — неинформативное, назови, например, `bot.py`.
- Добавь реальные команды калькулятора (`/calc`).

**Оценка: 3** — старт положен, теперь добавить функционал.
